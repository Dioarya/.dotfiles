return {
  "nickjvandyke/opencode.nvim",
  version = "*", -- Latest stable release
  dependencies = require "custom.configs.opencode.deps",
  config = require "custom.configs.opencode.config",
}
